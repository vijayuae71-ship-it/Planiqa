[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Edge Function — Claude API streaming proxy (NO timeout, unlike regular functions)
[[edge_functions]]
  path = "/api/claude"
  function = "claude"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
